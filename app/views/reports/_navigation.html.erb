<nav class="report-nav" aria-label="Accounting reports">
  <%= link_to "Trial balance", reports_path(tenant_route_options),
    class: controller_name == "reports" && action_name == "show" ? "report-nav__link report-nav__link--active" : "report-nav__link" %>
  <%= link_to "Profit & loss", profit_and_loss_report_path(tenant_route_options),
    class: action_name == "profit_and_loss" ? "report-nav__link report-nav__link--active" : "report-nav__link" %>
  <%= link_to "Balance sheet", balance_sheet_report_path(tenant_route_options),
    class: action_name == "balance_sheet" ? "report-nav__link report-nav__link--active" : "report-nav__link" %>
  <%= link_to "Aged receivables", aged_receivables_report_path(tenant_route_options),
    class: action_name == "aged_receivables" ? "report-nav__link report-nav__link--active" : "report-nav__link" %>
  <%= link_to "Aged payables", aged_payables_report_path(tenant_route_options),
    class: action_name == "aged_payables" ? "report-nav__link report-nav__link--active" : "report-nav__link" %>
  <%= link_to "Party ledger", party_ledger_report_path(tenant_route_options),
    class: action_name == "party_ledger" ? "report-nav__link report-nav__link--active" : "report-nav__link" %>
  <%= link_to "Day book", day_book_report_path(tenant_route_options),
    class: action_name == "day_book" ? "report-nav__link report-nav__link--active" : "report-nav__link" %>
  <%= link_to "GST summary", gst_summary_report_path(tenant_route_options),
    class: action_name == "gst_summary" ? "report-nav__link report-nav__link--active" : "report-nav__link" %>
  <%= link_to "Period close", period_close_path(tenant_route_options),
    class: controller_name == "period_closes" ? "report-nav__link report-nav__link--active" : "report-nav__link" %>
</nav>
